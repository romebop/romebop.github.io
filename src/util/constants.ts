const TICK_DURATION = 1000 / 30; // 1000 / 60
const INITIAL_DELAY = 8 * TICK_DURATION;
const INIT_DURATION = 5 * TICK_DURATION; // also DEINIT_DURATION
const CASCADE_DELAY = 2 * TICK_DURATION; // 1
const SLIDE_IN_DURATION = 5 * TICK_DURATION; // 9
const FLICKER_DURATION = 40 * TICK_DURATION;
const FILL_BAR_DURATION = 12 * TICK_DURATION;

const DEFAULT_EASING = 'ease-in-out';

const THICK_LINE_WIDTH = 14;
const THIN_LINE_WIDTH = 4;

export {
  CASCADE_DELAY,
  DEFAULT_EASING,
  FILL_BAR_DURATION,
  FLICKER_DURATION,
  INITIAL_DELAY,
  INIT_DURATION,
  SLIDE_IN_DURATION,
  THICK_LINE_WIDTH,
  THIN_LINE_WIDTH,
  TICK_DURATION,
};
